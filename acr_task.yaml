version: v1.1.0
steps:
- build: -t $Registry/{{.Run.TaskName}}:{{.Run.ID}} -t $Registry/{{.Run.TaskName}} -f Dockerfile .
# Push images
 - push:
  - $Registry/{{.Run.TaskName}}
  - $Registry/{{.Run.TaskName}}:{{.Run.ID}}